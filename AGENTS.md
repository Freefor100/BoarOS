# Agent 约束

本文只记录 BoarOS 特有的工作规则；Harness、工具和会话本身的通用安全规则由运行环境提供。

## 事实与取舍

- 开始修改前读 `README.md`、对应的 `docs/modules/`、源码和聚焦测试。模块文档是入口、不变量、限制和验证命令的索引；与源码或验证冲突时，以源码和验证为准，并在同一改动中修正文档。
- 涉及 Linux ABI、架构、设备、文件系统或比赛事实时，先读 `references/README.md` 与 `references/sources.tsv`，再使用其中固定的本地版本。记录结论时写明本地路径以及 commit、tag、文档版本或 SHA-256；只有固定资料缺失或不足以回答当前问题时才查官方资料，并记录 URL 和访问日期。
- 架构、ABI、所有权、依赖、范围或结构性性能取舍必须先调查事实，提出 2–3 个真实候选并比较正确性、演进、复杂度和成本，由人确认路线。只有一个可行方案时说明约束；已确认路线内的普通实现、编译修复和可逆小修继续完成，不反复制造候选。
- 不按测试名称、固定输入或预期输出写特判，不以成功存根掩盖缺失能力。内部实现可以不同于 Linux，但不能改变用户可观察语义，也不能用阶段性限制封死已确定的长期能力。

## 错误与所有权

- 用户参数、权限、格式和 ABI 失败返回规定的 errno 或用户 fault；资源耗尽、设备超时和真实 I/O 错误返回所属层的明确状态，并只在仍有真实 owner 且之后确实能恢复时保留清理状态。
- 合法 owner 的物理页、连续页和堆释放完成即返回；非法地址、重复或错误顺序释放、引用计数错误以及分配器元数据损坏触发 fatal trap，不建立跨层重试链，也不为故障注入保留这种状态。
- 真实 ext4/block I/O 失败由拥有它的 VFS mount 或对象保留。`close` 先摘 fd 和更新统计，再只处理选定 OFD；历史 cleanup 不改变本次有效 close 的结果。任务仍在使用自身内核栈时，任务页回收可以延后到可信栈。
- 设计和审查都要写清触发条件、契约、影响、owner 和证据；把正确性/ABI、安全与资源、并发、性能和风格建议分开。

## 验证与文档

- 先运行能证伪改动的最窄测试，再按依赖扩大到真实 U-mode、架构回归和完整 Harness。模块测试保护模块契约，真实程序保护组合 ABI；预期缺少能力的测例记录阻塞原因，不算回归。
- 测试保护用户可观察行为、稳定契约、不变量、所有权、并发或已记录的成本边界，不锁定私有布局和偶然调用顺序。实现变化后删除或改写失去独立检错价值的用例。
- 模块入口、接口、不变量、限制或验证方式变化时更新 `docs/modules/`；形成可复用的背景知识、固定资料依据、选择理由或调试经验时更新 `docs/learning/`；能力状态或近期方向变化时更新 `README.md`，依赖和阻塞清单维护在 `docs/goals.md`。没有新事实就不制造文档、目录或临时记录。
- 每个可独立验证的子系统阶段收口时检查 README、模块和 learning 三类文档。Harness 或 skill 的临时 plan/spec 不进仓库；跨 Session 保存前先确认用途和路径。
- `build/` 只长期保留能跨多轮复用的构建缓存；每阶段核对结果后用 `make prune-build` 清理运行目录、日志、镜像和旧缓存。Git 中记录结论与可重建命令，不把被忽略的本地证据当作永久档案。

## 提交边界

- 提交围绕一个可说明、可验证的问题，检查最终 diff 和用户已有修改。AI 有实质贡献时按 `CONTRIBUTING.md` 添加对应 `Co-authored-by` trailer，不提交会话材料、JSONL 或秘密。
- push、发布、比赛提交和阶段转换由人决定，除非人已明确授权；正常开发沿当前分支推进，实验性工作才另建分支或 worktree。
