# Agent 约束

本文只补充项目特有规则；Harness 已提供的工具、安全和会话能力不在这里重复。

## 开始工作

- 先读 `README.md`，再读本次修改涉及的源码、测试和模块说明。
- `docs/modules/` 是当前实现的导航和稳定契约索引。先从对应模块说明确认入口、职责、不变量、限制和聚焦测试，再用 `rg` 定位本次涉及的符号或 diff；不要因为需要核对一个局部事实而重新通读整个子系统。模块说明缺失、过时或与实现冲突时，以针对性源码和验证结果为准，并在同一工作范围内修正文档。
- 文档不能替代源码；修改前先确认当前事实和最窄验证方式。
- 涉及架构、Linux ABI、设备、文件系统或比赛事实时，先读 `references/README.md` 和 `references/sources.tsv`，再查对应的本地固定版本。结论应记录本地路径及 commit、tag、文档版本或 SHA-256；完整仓库切换分支后还要记录实际 commit。
- `references/` 既有完整仓库、浅层源码快照和单文件归档，源码快照还可能以 partial clone 或 sparse checkout 形式存在。本地没有某段历史、对象或工作树路径不能证明上游没有该内容；先按 `references/README.md` 检查并补全固定 commit 的对象或 sparse 路径。压缩包直接按成员读取，确需解包时放到仓库外或已忽略的构建目录，不把恢复结果、临时解包目录或手工网页快照加入 Git。
- 只有本地固定版本确实不含所需主题、官方后来发布的规则/勘误/板级资料是当前问题的必要输入，或固定对象无法恢复时，才回退到官方网站；优先标准组织、项目或厂商的一手资料，并记录 URL、版本与访问日期。不得把未固定的网页现状静默当作本地基线；若它与固定输入冲突，先说明差异并等待确认是否更新 `sources.tsv`。

## 设计与实现

- 项目长期目标是实现广泛的 Linux 用户态 ABI 兼容、Linux syscall 语义和完整内核生命周期，并在 QEMU 与目标开发板上取得有竞争力的性能。兼容性以用户态可观察的 ABI、错误码、数据结构、并发行为和资源生命周期为准；内部实现不要求照搬 Linux 源码结构。比赛分数、syscall 数量和局部测试通过都不能单独代表完成度；`ENOSYS` 不计作已实现能力。
- “当前需求”必须放在已经确定的长期路径中理解。`fork/clone/execve/wait`、多线程、SMP、VFS 与文件描述符、信号、阻塞唤醒、RISC-V/LoongArch 等已确定目标不属于假想需求；阶段测试通过也不是设计正确的充分条件。
- 在上述约束内选择最简单的最终可演进实现，不为尚未确定的需求增加层级、框架或通用接口。内部数据结构和算法可以比 Linux 简单，也可以暂不启用某项功能；但不得改变目标 ABI 语义、固化可退出的阶段限制或封死已确定能力。未启用 SMP 时可以不实现并发访问；启用共享 MM 或多核前必须落实同步、页引用和 TLB 协议。简化模型时说明适用边界、扩展路径和预期代价。
- 设计时明确区分永久机制、必要的过渡桥梁和纯占位。能以相近或更低风险直接形成最终路径时，跳过阶段性实现和占位；不为保持阶段完整而增加未使用接口、成功存根或空框架。
- 阶段按主线能力闭环划分，不按代码量、辅助层或提交数量划分。每个阶段必须交付一组可复用能力及其真实消费者，明确支持范围、正常与失败语义、所有权、回收和相关并发约束：从已有或同阶段建立的真实入口到可观察结果形成纵向闭环，或者完整承担一种可由后续主线直接消费的硬件、资源生命周期或独立失败域；实现应当进入非测试内核路径，并且不会在紧随其后的阶段被整体替换。仅新增未使用 API、测试专用链路、脚手架、空壳数据结构或为未来预留的对称接口，不构成阶段完成。
- 基础机制不能独立形成上述闭环时，必须与首个真实消费者在同一阶段设计、实现和收口；只有当拆分后的能力拥有可单独审查的不变量、错误语义和验证边界，并已被真实实现消费时才作为独立阶段。一个阶段可以包含多个可审查提交，单个提交或局部测试通过不等于阶段完成。
- 文件和目录按稳定职责、依赖方向与资源生命周期组织，不按行数或文件数机械拆分。只有同一领域已经出现多个长期实现单元、需要共享私有接口，或形成独立生命周期、失败域或验证边界时才增设子目录；拆分与触发它的主线能力同阶段完成，不预建空目录或进行仅改变路径的全局重排。单文件混合会独立演进的通用、架构或平台职责时，在下一次相关修改中沿稳定边界拆分。
- 过渡代码只在真实启动、硬件状态迁移或当前验证边界无法跳过时采用；必须隔离在明确边界内，不得扩散成通用接口或长期不变量，并说明验证方式和退出条件。新的稳定路径建立后，在当前工作范围内删除已失去作用的过渡代码；尚不能退出时将其记为模块当前限制。
- 性能是架构、接口和数据结构设计的一等约束。候选方案要比较目标工作负载下的热路径指令与访存、页表和 TLB 行为、复制次数、分配频率、锁竞争、缓存局部性及内存占用，避免把已知的结构性瓶颈固化进稳定接口。
- 不以“追求性能”为由提前堆叠复杂机制。复杂优化应由可重复基准、硬件计数或明确成本模型支持；尚未实现时要保留合理的优化路径。性能结论必须注明工作负载、平台和比较基线，不使用无法验证的“更快”或“性能优越”。
- 涉及架构、ABI、所有权、依赖、范围或结构性性能取舍时，Agent 先调查事实，给出 2–3 个真实候选，并比较正确性、长期演进、实现复杂度、性能成本和跨架构影响，等待人确认路线后再实现。普通实现、编译修复和已确认路线内的局部结构调整不反复制造候选。
- 只有一个可行方案时说明排除其他路线的约束，不制造陪衬方案。
- 明确、可逆且不引入新取舍的小修可以直接实现和验证。
- 已确认范围内的普通实现、编译和测试问题由 Agent 继续处理；出现新取舍、关键假设失效、范围扩大或依赖与许可变化时暂停讨论。
- 禁止按测试名称、固定输入或预期输出写特判；未实现能力返回准确错误，不伪造成功。

## 测试与审查

- 测试用于保护用户可观察的 Linux ABI、稳定模块契约、关键不变量、资源生命周期、并发语义或已经确认的性能退化边界。新增测试必须能够指出一种此前未覆盖的回归；不得按函数、分支、代码行或实现步骤机械堆叠用例，也不以测试数量或覆盖率百分比代替风险判断。
- 用等价类、边界值、状态转换和失败路径组织最小充分用例；同一层已有相同语义时优先扩展表驱动用例或现有 runner，不新建重复 fixture、镜像、启动链或测试目标。测试代码和 harness 的复杂度、运行时间与维护成本同样需要审查。
- 模块测试验证模块契约，架构或 QEMU 测试验证指令、页表、Trap 与设备边界，真实 U-mode/外部测例验证组合 ABI。只有各层能暴露不同失败域时才重复验证同一能力；低层白盒测试不能替代真实入口，端到端测试也不必枚举已经由低层穷尽的内部组合。
- 测试不得锁定私有结构布局、未承诺的辅助函数、偶然调用顺序、临时过渡状态或单一实现算法；除非这些内容本身就是已记录的 ABI、不变量、所有权协议、硬件约束或性能基线。稳定实现变化后，同一工作范围内合并、改写或删除已过时、重复且不再提供独立检错价值的测试。
- 测试失败只是需要调查的证据，不自动证明生产实现错误。先把失败还原为具体的 ABI、正确性、资源、并发、硬件或性能问题；外部测例与静态分析结果同样不得未经复核直接当作规范。
- 官方测例、未特改用户程序和真实运行配置用于验证组合 ABI；分别记录模块通过、真实入口通过和完整 Harness 通过。阶段顺序由内核目标与能力依赖图决定，官方测例不得作为开发路线或语义来源。具备依赖的测例应尽早运行；缺少能力的测例记录阻塞原因，不把预期能力缺口误报为回归，也不把测例输入当作功能上限。
- 审查结论必须区分正确性/ABI 错误、安全或资源风险、并发问题、性能问题和可维护性/代码风格建议。命名、排版、函数长度、个人偏好或启发式工具告警，除非违反项目明确规则或能证明会造成真实语义风险，不得报告成“错误实现”并阻塞主线。
- 审查问题必须说明触发条件、违反的契约、影响及证据；记录了限制不等于该限制合理。区分缺陷、能力缺口、性能风险和风格建议。只有确实可重试的失败才引入持久重试状态；故障注入不得为了制造任意失败而反过来强迫生产路径增加无依据的复杂度。
- 实现过程中运行能证伪当前改动的最窄测试；相关纵向闭环在能力完成时运行，完整架构回归在阶段收口、提交前或高风险跨模块修改后运行，不因每个局部编辑重复执行全套测试。
- 当任务仅为确认状态或验证回归、目标模块及依赖未出现在当前 diff、模块文档已明确稳定契约，且现有测试在正确层次直接覆盖待确认行为时，可以用该测试结果代替重新阅读未改动实现；必须记录实际运行入口和结果。设计或修改实现、审查代码质量，以及核对 ABI、所有权、资源回收、并发、安全、错误传播、性能热路径、未覆盖失败条件或硬件边界时，测试不能替代针对性源码阅读；测试失败、覆盖范围不明或文档与实现冲突时也必须定位到相关源码。

## 文档

文档分别保存项目事实和开发所需知识，只在事件产生可复用内容时更新：

- 模块入口、接口、不变量、限制或验证方式稳定变化：更新 `docs/modules/`。
- 当前阶段形成新的背景知识、确定的项目选择及理由、架构或板级资料依据、验证或调试经验：更新 `docs/learning/`。
- 当前能力或近期方向变化：更新 `README.md`。已确认能力、依赖和阻塞原因清单可以维护在 `docs/goals.md`，不能以“禁止需求盘点”为由回避长期目标。

每个可独立验证的子系统阶段收口时，Agent 必须主动检查上述三类文档，不等人再次提问。阶段是 DTB 读取、启动内存布局、物理页分配、分页等可单独理解和验证的能力，不是每个提交。

`docs/learning/` 是开发过程中形成的学习总结，供人之后集中复习。内容包括理解当前主题所需的背景知识、硬件或规范机制、术语，已经确定的项目选择及理由，经过核对的架构或板级事实与资料依据，以及可复用的验证和调试经验。只保留当前成立的结论；选择变化时直接更新或删除旧解释。不得写入未确认方案、TODO、临时 Agent plan、原始对话或按时间展开的流水账。稳定的项目接口和约束写入 `docs/modules/`，当前进度写入 `README.md`，跨模块工程原则写入 `docs/design.md`，演进过程由 Git 保存。

Harness 或 skill 要求的临时 spec、plan 和会话材料默认不属于项目文档。跨 Session 确实需要保留时，Agent 必须先征得人对具体用途和路径的同意。没有新事实就不制造文档、模板或目录。

## 完成工作

- 正常开发沿当前分支推进；主线之外的实验性功能才另建分支或 worktree。
- 先运行最窄检查，再按风险扩大；检查最终 diff，只报告实际结果。
- 修改形成或影响性能热路径时，除正确性测试外还要运行相称的性能基线或成本检查；当前环境无法可靠测量时，明确记录缺少的验证条件，不把推测写成结论。
- AI 有实质贡献时按 `CONTRIBUTING.md` 添加 `Co-authored-by`；不提交原始会话、JSONL 或秘密。
- push、发布、比赛提交和阶段转换由人决定，除非人已明确授权当前操作。
