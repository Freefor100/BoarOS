# Agent 约束

本文只补充项目特有规则；Harness 已提供的工具、安全和会话能力不在这里重复。

## 开始工作

- 先读 `README.md`，再读本次修改涉及的源码、测试和模块说明。
- 文档不能替代源码；修改前先确认当前事实和最窄验证方式。

## 设计与实现

- 选择满足当前需求的最简单正确实现，不为假想需求增加层级、框架或通用接口。
- 设计时明确区分永久机制、必要的过渡桥梁和纯占位。能以相近或更低风险直接形成最终路径时，跳过阶段性实现和占位；不为保持阶段完整而增加未使用接口、成功存根或空框架。
- 过渡代码只在真实启动、硬件状态迁移或当前验证边界无法跳过时采用；必须隔离在明确边界内，不得扩散成通用接口或长期不变量，并说明验证方式和退出条件。新的稳定路径建立后，在当前工作范围内删除已失去作用的过渡代码；尚不能退出时将其记为模块当前限制。
- 涉及架构、接口、不变量、算法、依赖、范围、性能或兼容性取舍时，Agent 先调查事实，给出 2–3 个真实候选及推荐理由，等待人确认路线后再实现。
- 只有一个可行方案时说明排除其他路线的约束，不制造陪衬方案。
- 明确、可逆且不引入新取舍的小修可以直接实现和验证。
- 已确认范围内的普通实现、编译和测试问题由 Agent 继续处理；出现新取舍、关键假设失效、范围扩大或依赖与许可变化时暂停讨论。
- 禁止按测试名称、固定输入或预期输出写特判；未实现能力返回准确错误，不伪造成功。

## 文档

文档分别保存项目事实和开发所需知识，只在事件产生可复用内容时更新：

- 模块入口、接口、不变量、限制或验证方式稳定变化：更新 `docs/modules/`。
- 当前阶段形成新的背景知识、确定的项目选择及理由、架构或板级资料依据、验证或调试经验：更新 `docs/learning/`。
- 当前能力或近期方向变化：更新 `README.md`。

每个可独立验证的子系统阶段收口时，Agent 必须主动检查上述三类文档，不等人再次提问。阶段是 DTB 读取、启动内存布局、物理页分配、分页等可单独理解和验证的能力，不是每个提交。

`docs/learning/` 是开发过程中形成的学习总结，供人之后集中复习。内容包括理解当前主题所需的背景知识、硬件或规范机制、术语，已经确定的项目选择及理由，经过核对的架构或板级事实与资料依据，以及可复用的验证和调试经验。只保留当前成立的结论；选择变化时直接更新或删除旧解释。不得写入未确认方案、TODO、临时 Agent plan、原始对话或按时间展开的流水账。稳定的项目接口和约束写入 `docs/modules/`，当前进度写入 `README.md`，跨模块工程原则写入 `docs/design.md`，演进过程由 Git 保存。

Harness 或 skill 要求的临时 spec、plan 和会话材料默认不属于项目文档。跨 Session 确实需要保留时，Agent 必须先征得人对具体用途和路径的同意。没有新事实就不制造文档、模板或目录。

## 完成工作

- 正常开发沿当前分支推进；主线之外的实验性功能才另建分支或 worktree。
- 先运行最窄检查，再按风险扩大；检查最终 diff，只报告实际结果。
- AI 有实质贡献时按 `CONTRIBUTING.md` 添加 `Co-authored-by`；不提交原始会话、JSONL 或秘密。
- push、发布、比赛提交和阶段转换由人决定，除非人已明确授权当前操作。
