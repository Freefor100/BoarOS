# 设计与工程原则

这里只记录跨模块的长期选择。当前能力见 [README](../README.md)，未完成工作和待确认路线见 [goals](goals.md)，操作规则见 [AGENTS](../AGENTS.md) 和 [CONTRIBUTING](../CONTRIBUTING.md)。

## 用户可观察契约

Linux 是 ABI 和机制参考，不是内部结构模板。返回值、errno、部分成功、对象身份、共享关系、并发可见性与生命周期必须符合声明范围；不以成功存根掩盖缺能力。真实未特改程序与固定 Linux 差分用于发现缺口，测例名称或 syscall 数量不定义路线和完成度。

## 最小边界与所有权

只为真实消费者、独立生命周期或失败域增加接口。VFS 的打开对象、inode、路径和挂载解决不同身份问题；共享映射的后备身份必须早于首次物理页分配。通用语义不随平台复制，第二架构/后端出现时再抽离具体差异。

每个机制明确发布点、失败回滚、睡眠期间 pin 和最终回收 owner。用户错误、资源耗尽、设备超时/I/O 错误返回所属层状态；分配器不变量损坏 fatal。只为仍有真实 owner 且能恢复的 I/O 保留清理状态。单 hart 关中断是当前同步条件，不能代替 SMP 锁、跨核唤醒和 TLB 完成确认。

## 成本与证据

对象选择准确和查找成本低是不同结论；缓存命中、复制、分配、锁、I/O 与 TLB 成本要分开测量。先建立正确性与可复现基线，再优化有证据的瓶颈。QEMU 功能结果不推导实板性能，关闭后可读不推导掉电持久性。
