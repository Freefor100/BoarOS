# 设计与工程原则

本文只记录跨模块、长期有效的工程选择。当前能力和限制以 `README.md`、
`docs/goals.md` 及模块契约为准。

## 语义与优先级

- Linux ABI 和本地固定资料决定语义：系统调用号、返回值、errno、数据结构、并发可见性和资源生命周期都属于用户可观察契约。内部结构可以比 Linux 简单，但不能用成功存根或阶段性限制掩盖缺口。
- 真实 Linux workload 决定兼容性缺口的优先级。先用未特改的 libc、BusyBox 和其他真实负载建立差分证据，再安排缺失 syscall 或细节；比赛测例用于发现和防止回归，不定义路线。
- 内存安全及核心所有权缺陷可先于 workload 修复。用户输入错误、资源耗尽、设备超时和真实 I/O 错误按所属接口返回；合法 owner 的分配器释放必须完成，非法释放、引用计数错误和分配器元数据损坏进入 fatal trap。只有真实 I/O 对象仍持有 owner 时才保留清理状态。

## 结构与演进

- 选择能直接成为最终路径的最小机制。通用代码、架构后端和平台差异按稳定职责分界；只在有真实消费者、独立生命周期或独立失败域时增加接口或目录。
- 永久机制、启动或硬件迁移所需的过渡桥梁和纯占位要分开。过渡代码限于明确边界，有验证和退出条件；稳定路径建立后删除失效桥梁。
- 每项能力从真实入口连接到可观察结果，并同时定义正常路径、失败语义、所有权、回收顺序和并发边界。当前单 hart 可以采用串行模型；引入共享 MM、SMP 或多核前必须补齐引用、锁和 TLB 协议。

## 性能与证据

性能是接口和数据结构的约束。比较方案时记录目标 workload、热路径指令和访存、TLB/页表行为、复制、分配、锁竞争和内存占用。只有可重复基准、硬件计数或明确成本模型支持的结论才能写成性能结论；QEMU 功能测试不代表开发板吞吐。

## 验证与记录

先运行能证伪改动的最窄测试，再按依赖扩大到真实 U-mode、架构回归和完整 Harness。测试保护 ABI、模块契约、关键不变量、所有权、并发和已记录的成本边界，不锁定私有布局或偶然调用顺序。

模块文档保存当前接口、不变量、限制和验证入口；learning 文档保存可复用的背景、固定资料依据、已确定选择和调试经验；README 保存能力状态和近期方向；目标依赖与阻塞项写入 `docs/goals.md`。没有新事实就不制造文档。重大架构、ABI、所有权、依赖、范围或结构性性能取舍先调查并比较真实候选，由人确认后实施；push、发布和比赛提交由人决定。
